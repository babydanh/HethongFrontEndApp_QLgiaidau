# 📋 Session Claude Code — 30/06/2026

> **Dự án:** Quản Lý Giải Đấu (Tournament Management System)
> **Branch:** main
> **Commit cuối:** `c88e134 restore: phuc hoi code tu backup-unpushed`
> **APK file:** `app_quanly_giaidau/build/app/outputs/flutter-apk/app.apk`

---

## 🛠 Những Gì Đã Làm Trong Session

### 1. App Responsive Architecture
- **Tạo:** `core/widgets/app_responsive.dart`
- Breakpoints: mobile (<600px) | smallTablet (600-839) | tablet (≥840)
- Helpers: `AppResponsive.gridDelegate()`, `sidebarWidth()`, `bannerHeight()`
- `AppResponsiveBuilder` widget builder
- Extension: `context.isMobile`, `context.isTablet`
- **Update:** `docs/SKILLS.md` — thêm mục 8.8 Responsive Architecture

### 2. Filters & Segments
- **Tạo:** `core/widgets/sport_filter_chips.dart` — chips lọc môn thể thao
- **Tạo:** `core/widgets/status_segment.dart` — segment tabs: Tất cả/Đăng ký/Đang diễn ra/Đã kết thúc

### 3. Notification Feature
- **Tạo:** `domain/entities/app_notification.dart` — entity với type→icon→color mapping
- **Tạo:** `data/repositories/api/api_notification_repository.dart` — GET list, unread, PATCH read/read-all
- **Tạo:** `providers/notification_provider.dart` — Riverpod Notifier + unreadCountProvider
- **Tạo:** `features/notification/screens/notification_screen.dart` — list, group by date, infinite scroll

### 4. Community/Club Feature
- **Tạo:** `domain/entities/community.dart` — từ API `/communities`
- **Tạo:** `data/repositories/api/api_community_repository.dart` — getCommunities, getMyCommunities, getById
- **Tạo:** `providers/community_provider.dart` — communitiesProvider, communityDetailProvider
- **Tạo:** `features/community/screens/club_detail_screen.dart` — banner + tabs (Giới thiệu/Giải đấu/Thành viên/BXH)

### 5. Cài đặt (Settings)
- **Tạo:** `features/profile/screens/settings_screen.dart` — 3 tabs (Hồ sơ/Ngân hàng/Bảo mật)
- **Thêm route:** `/profile/settings`

### 6. Bracket Diagram (graphview)
- **Tạo:** `features/bracket/screens/bracket_diagram_screen.dart`
- Dùng `GraphView` + `BuchheimWalkerAlgorithm` + `BuchheimWalkerConfiguration`
- `BracketGraphService.buildSingleEliminationGraph()` — tạo graph từ matches
- `InteractiveViewer` zoom/pan
- Auto-rotate landscape

### 7. Ranking Widgets
- **Tạo:** `features/rankings/widgets/podium_view.dart` — top 3 (🥇🥈🥉)
- **Tạo:** `features/rankings/widgets/ranking_row.dart` — rank 4+
- **Tạo:** `features/rankings/widgets/user_stats_card.dart` — card gradient + progress bar
- **Update:** `leaderboard_screen.dart` — dùng widget mới + API thật

### 8. Match Card
- **Sửa:** `core/widgets/match_card/match_card_detail.dart` — layout dọc Team A/B + sets bên phải
- Status: 🔴 LIVE / 🟢 HOÀN TẤT / ⚪ SẮP ĐÁ
- Giờ + Sân + score + sets detail

### 9. Bỏ Mock Data
- **Sửa:** `providers/ranking_provider.dart` — bỏ mockRankingsProvider, dùng API thật
- **Sửa:** `data/repositories/api/api_ranking_repository.dart` — fix endpoint `/rankings/user/:userId`
- **Sửa:** `domain/entities/ranking.dart` — thêm `categoryId`
- **Sửa:** `api_ranking_repository.dart` — parse `publicRanks` đúng response

### 10. Tournament Card
- **Tạo:** `features/home/widgets/tournament_card_with_banner.dart` — banner 140px + status badge
- **Sửa:** `home_screen.dart` — thêm sport filter + status filter + search
- **Sửa:** `features/tournament/screens/tournament_intro_screen.dart` — expandable team cards + ELO

### 11. Router
- **Sửa:** `core/router/app_router.dart` — thêm route:
  - `/notifications` → NotificationScreen
  - `/club/:id` → ClubDetailScreen
  - `/profile/settings` → SettingsScreen
  - `/profile` cho phép khi chưa login

### 12. Navigation
- **Sửa:** `floating_bottom_nav.dart` — label: Câu lạc bộ, Bảng xếp hạng
- **Sửa:** `home_screen.dart` — fix withOpacity → withValues

---

## 📁 Toàn Bộ Files Đã Tạo

```
lib/
├── core/widgets/
│   ├── app_responsive.dart                ← MỚI
│   ├── sport_filter_chips.dart            ← MỚI
│   └── status_segment.dart                ← MỚI
├── domain/entities/
│   ├── community.dart                     ← MỚI
│   └── app_notification.dart              ← MỚI
├── data/repositories/api/
│   ├── api_community_repository.dart      ← MỚI
│   └── api_notification_repository.dart   ← MỚI
├── providers/
│   ├── community_provider.dart            ← MỚI
│   ├── notification_provider.dart         ← MỚI
│   └── ranking_provider.dart              ← SỬA
├── features/notification/screens/
│   └── notification_screen.dart           ← MỚI
├── features/community/screens/
│   └── club_detail_screen.dart            ← MỚI
├── features/bracket/screens/
│   └── bracket_diagram_screen.dart        ← MỚI
├── features/rankings/widgets/
│   ├── podium_view.dart                   ← MỚI
│   ├── ranking_row.dart                   ← MỚI
│   └── user_stats_card.dart               ← MỚI
├── features/profile/screens/
│   └── settings_screen.dart               ← MỚI
├── features/home/widgets/
│   └── tournament_card_with_banner.dart   ← MỚI
└── core/router/
    └── app_router.dart                    ← SỬA
```

---

## 🔴 Vấn Đề Còn Tồn Đọng

### Lỗi API - Ranking
- `api_ranking_repository.dart:getUserRank()` gọi `/rankings/user/$userId/rank/$categoryId` — **sai endpoint**
- **Đã fix:** ✅ thành `/rankings/user/$userId?categoryId=...`, parse `publicRanks`

### Mock Còn Trong home_screen.dart
- `_mockNotifications = 3` — chưa được thay bằng unreadCountProvider
- `_mockElo`, `_mockWins`, `_mockLosses`, `_mockTotal` — còn mock trong header
- **Cần fix:** thay bằng `userRankProvider` và `unreadCountProvider`

### File Chưa Được Tạo Lại (bị mất do git checkout)
- `core/widgets/match_card/match_card_detail.dart` — cần tạo lại
- `features/home/widgets/tournament_card_v2.dart` — cần tạo lại
- `docs/` — tất cả file plan (.md) cần tạo lại

---

## 📝 Git Commands Quan Trọng

```bash
# Xem lịch sử
git reflog

# Xem stash
git stash list

# Xem commit chưa push
git log origin/main..HEAD

# Tạo branch backup
git branch backup-unpushed <commit-hash>

# Khôi phục code từ stash
git stash pop stash@{1}

# Reset hard về commit cũ (cẩn thận!)
git reset --hard <commit-hash>
```

---

## 🆘 Cứu Code Từ APK (Nếu Cần)

```bash
# Điều kiện: APK build = flutter run (debug mode)
pip install reflutter
adb shell pm path com.example.app_quanly_giaidau
adb pull <đường-dẫn> app.apk
reflutter app.apk
# → Output chứa kernel_blob.bin → ~85% source gốc
```

---

## 🎯 Việc Cần Làm Tiếp Theo

| Ưu tiên | Việc | Mức độ |
|:-------:|------|:------:|
| 1 | Fix nốt mock trong `home_screen.dart` | ⭐ |
| 2 | Tạo lại `match_card_detail.dart` | ⭐ |
| 3 | Tạo lại `tournament_card_v2.dart` | ⭐ |
| 4 | Tạo lại các file `docs/` plan | ⭐⭐ |
| 5 | Import Excel UI | ⭐⭐ |
| 6 | Create Tournament Wizard | ⭐⭐⭐ |
| 7 | Socket.IO realtime | ⭐⭐⭐ |
| 8 | Unit test | ⭐⭐⭐ |
